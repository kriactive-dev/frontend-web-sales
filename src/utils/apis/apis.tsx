const baseUrls = 'https://api-demo.websales-mz.com/api/'

const urls = {
    user: `${baseUrls}users`,
    permissions: `${baseUrls}permissions`,
    roles: `${baseUrls}roles`,
};

export default urls;
